# ===========================
# Gasolinera ERP - Backend
# ===========================

APP_NAME="Gasolinera ERP"
APP_ENV=production
APP_DEBUG=false
APP_URL=http://localhost:8080
APP_TIMEZONE=America/Guatemala

# Base de Datos MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=gasolinera_erp
DB_USERNAME=root
DB_PASSWORD=secret

# JWT (IMPORTANTE: cambiar en producción)
# Generar con: php -r "echo bin2hex(random_bytes(32));"
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_64_CHAR_HEX_STRING
JWT_ACCESS_TTL=3600
JWT_REFRESH_TTL=604800

# CORS
CORS_ORIGINS=http://localhost:3000
